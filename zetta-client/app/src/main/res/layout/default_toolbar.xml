<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.AppBarLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:custom="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="120dp"
  android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

  <android.support.v7.widget.Toolbar

    android:id="@+id/toolbar"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="?android:attr/actionBarSize"
    android:theme="@style/ToolbarTheme"
    custom:layout_scrollFlags="scroll|enterAlways"
    custom:popupTheme="@style/ThemeOverlay.AppCompat.Light"
    custom:subtitleTextAppearance="@style/Toolbar.SubtitleText"
    custom:titleTextAppearance="@style/Toolbar.TitleText" />

  <TextView
      android:id="@+id/toolbar_title"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:padding="12dp"
      android:layout_gravity="center"

      android:text="VITALS"
      android:textColor="@color/colorTextIcons"
      android:textSize="30sp" />

</android.support.design.widget.AppBarLayout>
