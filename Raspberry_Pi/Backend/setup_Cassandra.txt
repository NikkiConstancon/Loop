1:
 curl -L http://downloads.datastax.com/community/dsc-cassandra-3.0.5-bin.tar.gz | tar xz

2:
 got to "dsc-cassandra-3.0.5/bin"

3:
 Open terminal, "sudo ./cassandra"

4:
 "sudo ./cqlsh"

//Note: remember ; at the end of commands- ... could mean you forgot a ;
Create keyspace (DB):
cqlsh> create keyspace users with replication = {'class':'SimpleStrategy','replication_factor':1};


https://academy.datastax.com/resources/getting-started-apache-cassandra-and-nodejs



6: create table:
CREATE TABLE users (
  id varchar PRIMARY KEY,
  email varchar,
  name varchar,
  surname varchar
);


--- need to have node installed ---> sudo apt-get install node
5: nmp install  cassandra-driver
7: run database manager: node databasemanager.js 


